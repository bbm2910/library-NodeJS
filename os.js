const os = require('os'); //Find info about the operating system

// console.log(os); //Gets an object 
console.log(os.platform()); //Gets the platform that we are using
console.log(os.homedir()); //Gets the home directory